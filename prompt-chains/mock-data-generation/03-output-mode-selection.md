# Mock Data Generation Chain - 03 Output Mode Selection

Continue following `procedures/mock_data_generator/SKILL.md`.

Select the output mode for the mock data artifact based on the schema,
dependency plan, and my local loading workflow.

Preferred output mode:

```text
<paste one of: SQL INSERT statements, PL/SQL block, Python generator, CSV files, or undecided>
```

Local loading constraints:

```text
<paste transaction, sequence, file, delimiter, environment, or review constraints here>
```

Return:

1. Recommended output mode.
2. Why it fits the dependency and loading constraints.
3. Oracle reference drawer to consult, if PL/SQL or Oracle idioms are needed.
4. Assumptions to confirm before final generation.

# Mock Data Generation Chain - 04 Data Generation

Continue following `procedures/mock_data_generator/SKILL.md`.

Generate the mock data artifact now using the confirmed schema, dependency
order, row counts, realism rules, and output mode.

Confirmed output mode:

```text
<paste confirmed output mode here>
```

Final generation constraints:

```text
<paste final row counts, fixed values, date ranges, domain values, nullability, uniqueness, and naming rules here>
```

Return exactly one artifact style. Include:

1. The generated artifact content.
2. Table generation order.
3. Row-count summary.
4. Assumptions and unresolved gaps.
5. Validation checks I should run locally.

Use `references/oracle-sql/sections/plsql_basics.md` or
`references/oracle-sql/sections/oracle_idioms.md` only when the chosen artifact
requires those Oracle details.
